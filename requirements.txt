# Web
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic-settings>=2.6.0

# Agent core
langgraph>=0.2.50
langchain-core>=0.3.20
langchain-community>=0.3.20
langchain-ollama>=0.2.0
langchain-openai>=0.2.14

# Long-term semantic memory (local-first; swap for Qdrant later via MemoryStore protocol)
chromadb>=0.5.20

# Lights manifest (data/lights.yaml)
pyyaml>=6.0.2

# HTTP client used by some integrations
httpx>=0.27.0

# YouTube captions for lights (no YouTube Data API key)
youtube-transcript-api>=1.0.0

# Local TTS (Kokoro ONNX — CPU-friendly)
kokoro-onnx>=0.5.0
soundfile>=0.12.0
