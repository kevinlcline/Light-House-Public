[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PYTHONPATH=src uvicorn light_house.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/login"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
